// Windows-only facade for the Win32 Dart API.
//
// Shared code should depend on a platform service where one exists. This
// facade is retained for the existing Windows feature implementation while
// that feature graph is migrated behind the Phase 2 contracts.
export 'package:win32/win32.dart';
